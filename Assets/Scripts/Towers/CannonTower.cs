using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CannonTower : MonoBehaviour
{
    // public float range = 3f;
    // public LayerMask enemiesLayer;
    // public List<EnemyController> enemiesInRange = new List<EnemyController>();
    // public List<EnemyController> enemiesList = new List<EnemyController>();
    // public Collider[] collidersInRange;

    // private float checkCounter;
    // public float checkTime = 0.2f;
    // Start is called before the first frame update
    void Start()
    {
        // checkCounter = checkTime;
    }

    // Update is called once per frame
    void Update()
    {
        // checkCounter -= Time.deltaTime;
        // if (checkCounter <= 0)
        // {
        //     checkCounter = checkTime;
        //     // collidersInRange = Physics.OverlapSphere(transform.position, range, enemiesLayer);

        //     // enemiesInRange.Clear();
        //     // foreach (Collider col in collidersInRange)
        //     // {
        //     //     enemiesInRange.Add(col.GetComponent<EnemyController>());
        //     // }
        // }

    }
}
